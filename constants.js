const DEFAULT_RATE_LIMIT = 10;
const DEFAULT_RATE_LIMIT_WINDOW = 1 * 60 * 1000; // 1 minute 

const DEFAULT_PORT = 3000;

const UPLOAD_DIR = './uploads';
const UPLOAD_PROP = 'file';

module.exports = {
    DEFAULT_RATE_LIMIT,
    DEFAULT_RATE_LIMIT_WINDOW,
    DEFAULT_PORT,
    UPLOAD_DIR,
    UPLOAD_PROP
}